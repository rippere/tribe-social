# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
.env.*
!.env.example

# Data / media (large binary files, not source)
# Note: scores.csv and engagement.csv are intentionally tracked — they power the demo
reels/
media/
*.mp4
*.mov

# RunPod session scripts with embedded keys
runpod_session.sh
